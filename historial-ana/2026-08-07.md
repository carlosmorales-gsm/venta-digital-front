# ANA — 2026-08-07 — Frontend Venta Digital

## Objetivo del día

Enriquecer la captura de venta del vendedor con planes reales de Odoo,
financiamiento y ubicación de parque; afinar la vista previa PDF.

## Planes de Odoo en la captura

- Nuevo `SalePlanSearchModal.vue`: busca contra `GET /odoo/planes`
  (`planKind` = `PARQUE` | `PLAN_FUTURO`, `q`, `limit`).
- Al seleccionar plan se llenan `nombrePlan`, `productId` (id `product.template`)
  y `productDefaultCode` (referencia interna, **solo informativa**, no sustituye
  al `productId`).
- `precioPlan` regresa como solo lectura y alimenta el precio en la pestaña Pago.

## Pestaña Plan

- Sub-pestañas: **Plan** y **Financiamiento**.
- El **anticipo** se captura en la pestaña Plan; en Pago se muestra solo el label.
- `preasignacion` es bandera booleana: si está activa se muestran, en orden,
  **Parque → Sección → Cuadrante → Ubicación**.
- Al cambiar tipo de plan o desactivar preasignación se vacían los campos que
  dejan de aplicar (evita datos colgados de un tipo anterior).

## Vista previa PDF

- Se corrige la firma en la vista previa cuando el flujo está completado.

## Pendiente / siguiente

- Persistir `productId` real en la conciliación de Odoo (para la cotización).
- Confirmar mapeo de financiamiento contra `sale.order` en Odoo.
