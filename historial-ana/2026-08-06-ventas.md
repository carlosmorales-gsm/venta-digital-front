# ANA — 2026-08-06 (ventas vendedor)

## Carátula de contrato

Formulario basado en `CARATULA DE CONTRATO AGOSTO 2026`.

## Front

- Listado `/vendedor/ventas`: Nueva venta, borradores, ojo (vista previa PDF).
- Captura wizard móvil/tablet: `/vendedor/ventas/nueva` y `/vendedor/ventas/:id`.
- Borradores: máx. 3, 24 h.
- Reutilizar cotización (checks: contacto, 2.º contacto, derechohabientes).
- Vista previa PDF en `VdModal` (`SalePdfPreviewModal` + `jspdf`).
- Envío exige INE + comprobante de domicilio.

## Backend

- Tabla `vd_sales` + APIs draft/submit/list/referencias.
