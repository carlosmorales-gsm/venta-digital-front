# ANA — 2026-08-10 — Frontend Venta Digital

## Objetivo del día

Filtros de listados (monitor y vendedor), alineación de orígenes de venta con
Odoo y reconstrucción del buscador de planes/preasignación perdido.

## Filtros en listados (monitor y vendedor)

- Nuevo `utils/sales-list-filters.ts` centraliza la lógica:
  - `lastWeekRange`, `matchesDateRange`, `localDateKey`, `toLocalDateInputValue`.
  - `normalizeSearchText`, `textIncludesNormalized`, `textEqualsNormalized`.
- Filtro por **fechas**: por defecto la última semana.
- Filtro por **cliente**: select autocompletable.
- Búsqueda **insensible a acentos** (`normalize('NFD')` + quita diacríticos),
  p. ej. "e" coincide con "é".
- Aplicado en `MonitorSalesView.vue` y `SellerSalesView.vue`.

## Orígenes de venta

- `constants/sale-origins.ts` alineado con el selection `sale_origin` de
  `sale.order` en Odoo (mismos `value`; label en español para la UI).

## Captura: plan y preasignación (reconstrucción)

- Se reintegró `SalePlanSearchModal` en `SellerSaleCaptureView.vue` (se había
  perdido el 10 al restaurar desde git un archivo que quedó en 0 bytes; el
  trabajo del 7 nunca llegó a commit).
- Botón "Buscar en Odoo" junto al nombre del plan; muestra "Plan Odoo #id ·
  código · precio". Editar el nombre a mano deshace la liga con el producto.
- **Preasignación** ahora es checkbox; solo aparece en tipo Parque y despliega
  Parque → Sección → Cuadrante → Ubicación. Cambiar tipo/quitar preasignación
  limpia los campos.

## Notas

- Los ajustes de Odoo (conciliación, asociar/generar cliente, generar
  cotización) viven en el repo `C:\desarrollo\sanmartin\odoo` (módulo
  `sale_order_reception`).

## Pendiente / siguiente

- Commit del trabajo reconstruido para no volver a perderlo.

---

## Actualización tarde / noche (misma fecha, no documentado a tiempo)

- Restauración progresiva de `SellerSaleCaptureView` (servicio funerario solo
  plan futuro, mock dev, financiamiento, descuento entero).
- Trabajo posterior movido al ANA **2026-08-11** (pago efectivo, asesor label,
  ubicación).
