# ANA — 2026-08-11 — Frontend Venta Digital

## Objetivo del día

Completar captura/pago (ubicación, efectivo, asesor) y alinear validaciones del
modal de pago con el backend.

## Captura de venta

- **Nombre del asesor:** deja de ser input; muestra el `fullName` del vendedor
  logueado y se sincroniza al guardar borrador/finalizar (`SellerSaleCaptureView`).
- **IDs de ubicación:** el formulario ya envía `parkId`, `sectionId`,
  `quadrantId`, `spaceId` en `ubicacionPlan` (requiere backend desplegado con
  DTO/entidad actualizados).

## Búsqueda de ubicación (Odoo)

- `SaleLocationSearchModal`: al listar **cuadrantes** envía también `parkId`
  (además de `sectionId`), acorde al filtro en api-odoo-gsm/backend.
- Secciones y cuadrantes deben ser solo los que tienen espacios vendibles en el
  parque seleccionado (criterio en api-odoo-gsm).

## Modal Registrar pago (`SalePaymentModal`)

- **EFECTIVO:** oculta cuenta/banco; pide **efectivo recibido** y calcula
  **cambio** (recibido − anticipo). Guarda `montoRecibido` y `cambio`.
- **TRANSFERENCIA:** exige cuenta + banco (y nombre si banco = Otro).
- **CHEQUE:** exige banco; no pide cuenta.
- Forma de pago obligatoria; anticipo > 0; botón Guardar deshabilitado si no
  cumple reglas (espejo del backend).

## Ticket de pago PDF

- En efectivo imprime **Recibido** y **Cambio** en lugar de cuenta/banco
  (`payment-ticket-pdf.ts`).

## Tipos

- `sale-form.ts`: campos `montoRecibido`, `cambio` en `pago`.

## Pendiente / siguiente

- Redesplegar backend con columnas `monto_recibido`, `cambio` y fix de
  `parkId`… en DTO.
- Commit del trabajo acumulado (10–11).
